services:
  steam-server-ui:
    container_name: steam-server-ui
    build:
      context: ..
      dockerfile: ./.docker/Dockerfile
    image: steamserverui:latest
    stdin_open: true
    tty: true
    deploy:
      resources:
        limits:
          cpus: '8'
          memory: 16G
        reservations:
          cpus: '4'
          memory: 8G
    ports:
      - "4545:8443"
      - "17016:27016/udp"
      - "17016:27016/tcp"
      - "17015:27015/udp"
      - "17015:27015/tcp"
    volumes:
      - app-data:/app
      - ./saves:/app/saves:rw
      - ./SSUI:/app/SSUI:rw
    restart: unless-stopped

volumes:
  app-data:
